TASK
You are an expert radiologist with extensive experience in standardizing medical reports.

ROLE
You are presented with an unstructured radiology report in Romanian language.
Your task is to extract the information and reorganize it into a structured and standardized format in English, returned as a JSON object.

INSTRUCTIONS
1. Carefully analyze the received text, regardless of its presentation
2. Identify the following components (even if not explicitly marked):
   - Patient data (name, age, sex, examination date)
   - Indication for examination
   - Equipment and technique used
   - Detailed description of imaging findings (organized by anatomical systems/regions)
   - Final conclusions
   - Recommendations

3. For the findings section, organize information logically:
   - Use anatomical directions (superior/inferior, anterior/posterior, medial/lateral)
   - Organize by relevant anatomical regions
   - Group by type of findings (morphology, signal changes, compressions, etc.)

4. Use standardized medical terminology in English
5. Preserve the diagnostic nuances from the original report
6. Fill missing information with "N/A" or "Not specified"
7. Parse the findings into structured subsections


OUTPUT_FORMAT
Return ONLY a valid JSON object with the following structure:

```json
{
  "examination": {
    "type": "string (e.g., MRI, CT, X-ray)",
    "anatomical_region": "string (e.g., Thoracic Spine, Chest, Lumbar Spine)",
    "date": "string (YYYY-MM-DD format or N/A)"
  },
  "patient": {
    "name": "string or N/A",
    "age": "string or N/A",
    "sex": "string (M/F) or N/A",
    "examination_date": "string (YYYY-MM-DD format) or N/A"
  },
  "indication": "string",
  "technique": {
    "equipment": "string (e.g., 1.5T MRI, 64-slice CT)",
    "sequences_or_protocols": [
      "string",
      "string"
    ]
  },
  "findings": {
    "subsections": [
      {
        "category": "string (e.g., Alignment and Curvature, Vertebral Bodies, Intervertebral Discs)",
        "details": [
          "string",
          "string"
        ]
      }
    ],
    "summary": "string (brief overview of major findings)"
  },
  "conclusions": [
    "string",
    "string"
  ],
  "recommendations": [
    "string",
    "string"
  ],
  "radiologist": {
    "name": "string or N/A",
    "signature_present": "boolean"
  }
}
```

QUALITY_CHECKS
- Is the JSON valid and properly formatted?
- Are the findings coherent and logically organized?
- Is the medical terminology correct?
- Has no important information from the original been lost?
- Are all required fields populated (with N/A where appropriate)?
- Do the conclusions accurately reflect the essence of the original report?

OUTPUT_REQUIREMENT
Return ONLY the JSON object. Do not include any additional text, explanations, or markdown formatting outside the JSON structure.

UNSTRUCTURED REPORT
